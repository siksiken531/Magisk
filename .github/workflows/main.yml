name: Build Magisk Nova

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build Magisk NDK and Binaries
        run: |
          export ANDROID_HOME=$ANDROID_SDK_ROOT
          export ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT
          
          # Sürüm kodunu 328000 yaparak macro çakışmalarını önle
          sed -i 's/#define MAGISK_VER_CODE 31000/#define MAGISK_VER_CODE 328000/g' src/include/consts.hpp || true

          python3 build.py ndk
          python3 build.py all

      - name: Assemble Release APK
        run: |
          export ANDROID_HOME=$ANDROID_SDK_ROOT
          export ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT
          ./gradlew assembleRelease

      - name: Upload Magisk Release Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Magisk-Release
          path: |
            out/*.apk
            app/apk/build/outputs/apk/release/*.apk
            
